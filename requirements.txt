


gradio>=4.0.0
PyPDF2
google-generativeai
tensorflow # Or tensorflow-cpu if preferred
transformers
torch # Required by transformers
numpy
SpeechRecognition
pyttsx3 # Might still be needed if used elsewhere, though gTTS is preferred for Gradio
gTTS
pygame
python-dotenv
matplotlib
# firebase-admin # Keep if needed for backend auth, though frontend JS SDK is common with Gradio